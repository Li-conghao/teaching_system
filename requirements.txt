# 本科教学管理系统 - 依赖包

# 数据可视化（可选）
matplotlib>=3.5.0

# 数据处理（可选，用于数据导出和分析）
pandas>=1.3.0

# 注意：
# - tkinter 是Python标准库，无需安装
# - sqlite3 是Python标准库，无需安装
# - socket 是Python标准库，无需安装
# - threading 是Python标准库，无需安装
# - json 是Python标准库，无需安装
# - hashlib 是Python标准库，无需安装

# 如果需要数据可视化功能，请安装：
# pip install matplotlib pandas

# 或者直接安装所有依赖：
# pip install -r requirements.txt